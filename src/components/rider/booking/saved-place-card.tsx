import { Briefcase, Home, MoreHorizontal, Star } from 'lucide-react';
import { Button } from '@/components/ui/button';
import type { DemoPlace } from '@/lib/demo/location-suggestions';
function Icon({ tag }: { tag?: DemoPlace['tag'] }) { if (tag === 'home') return <Home className="h-5 w-5" />; if (tag === 'work') return <Briefcase className="h-5 w-5" />; return <Star className="h-5 w-5" />; }
export function SavedPlaceCard({ place, onSelect, editable = false }: { place: DemoPlace; onSelect?: (place: DemoPlace) => void; editable?: boolean }) { return <button type="button" onClick={() => onSelect?.(place)} className="flex w-full items-center gap-3 rounded-2xl bg-white p-4 text-left shadow-sm"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-primary/10 text-primary"><Icon tag={place.tag} /></div><div className="flex-1"><p className="font-bold">{place.name}</p><p className="text-sm text-muted-foreground">{place.address}</p>{place.note ? <p className="text-xs text-muted-foreground">{place.note}</p> : null}</div>{editable ? <Button type="button" variant="ghost" size="icon" className="rounded-full"><MoreHorizontal className="h-5 w-5" /></Button> : null}</button>; }
