import { Clock, MapPin } from 'lucide-react';
import type { DemoPlace } from '@/lib/demo/location-suggestions';
export function RecentSearchList({ title = 'Recent searches', places, onSelect }: { title?: string; places: DemoPlace[]; onSelect?: (place: DemoPlace) => void }) { return <section className="space-y-2"><h2 className="px-1 text-sm font-extrabold uppercase tracking-wide text-muted-foreground">{title}</h2>{places.map((place) => <button key={place.id} type="button" onClick={() => onSelect?.(place)} className="flex w-full items-center gap-3 rounded-2xl bg-white p-4 text-left shadow-sm"><div className="flex h-10 w-10 items-center justify-center rounded-2xl bg-slate-100 text-primary">{place.tag === 'recent' ? <Clock className="h-5 w-5" /> : <MapPin className="h-5 w-5" />}</div><div><p className="font-bold">{place.name}</p><p className="text-sm text-muted-foreground">{place.address}</p></div></button>)}</section>; }
