import { RouteShellPage } from '@/components/route-shell-page';

export default function WalletPage() {
  return (
    <RouteShellPage
      eyebrow="Route preview"
      title="Wallet"
      description="Wallet balances are not real stored value."
    />
  );
}
