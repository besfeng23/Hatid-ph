import { RoutePlaceholder } from '@/components/route-placeholder';

export default function AccountPage() {
  return (
    <RoutePlaceholder
      title="Account"
      description="Account settings will manage saved profile details and preferences once profile UI wiring is complete."
      status="No live account mutation, role assignment, or privileged profile update is active in this phase."
    />
  );
}
