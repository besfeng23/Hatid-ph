import { RouteShellPage } from '@/components/route-shell-page';

export default function SafetyPage() {
  return (
    <RouteShellPage
      eyebrow="Route preview"
      title="Safety"
      description="Safety flows are not live emergency services."
    />
  );
}
