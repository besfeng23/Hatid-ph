import { RouteShellPage } from '@/components/route-shell-page';

export default function TripsPage() {
  return (
    <RouteShellPage
      eyebrow="Route preview"
      title="Trips"
      description="Trips are not connected to live booking records yet."
    />
  );
}
