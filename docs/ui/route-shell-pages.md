# Route shell pages

This change adds standalone Next.js route shells for QA and navigation coverage:

- `/trips`
- `/wallet`
- `/safety`
- `/account`

These pages are intentionally lightweight. They prevent missing-route errors while the real data flows are built separately.
